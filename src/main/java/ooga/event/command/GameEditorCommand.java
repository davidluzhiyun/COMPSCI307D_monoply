package ooga.event.command;

/**
 * Created when player chooses to go to the game board editor within the GameSelectionScene screen.
 * Does not really need any info
 */
public class GameEditorCommand implements Command{

  @Override
  public Object getCommandArgs() {
    return null;
  }
}
